/*
 *
 *
 *
 *
 * mmath.h
 *
 *
 *
 *
 *
 */

#ifndef HAS_MMATH_H
#define HAS_MMATH_H

#define min(a, b) ((a) < (b) ? (a) : (b))
#define max(a, b) ((a) > (b) ? (a) : (b))
#define clamp(a, b, c) min(max(a, b), c)

#endif /* HAS_MMATH_H */
